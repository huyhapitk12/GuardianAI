## 🚀 Bắt đầu nhanh

### 1. Cài đặt

Thực hiện các bước sau để tải về và cài đặt các thư viện cần thiết:

```bash
git clone https://github.com/huyhapitk12/GuardianAI.git
cd GuardianAI
pip install -r requirements.txt
```

> **Lưu ý:** Đảm bảo bạn đã cài đặt Python và Git trên máy tính của mình.

### 2. Chạy Code

Sau khi cài đặt thành công, bạn có thể chạy ứng dụng bằng lệnh sau:

```bash
python main.py
```
## Biến môi trường

Để chạy dự án này, bạn sẽ cần thêm các biến môi trường sau vào tệp .env của mình

`API_KEY`
